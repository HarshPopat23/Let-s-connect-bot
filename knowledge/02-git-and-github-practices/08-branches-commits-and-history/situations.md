---
title: Branches, commits, and history
group: 02-git-and-github-practices
category: 08-branches-commits-and-history
journey_stage: contribute
scenario_count: 50
content_type: situations-only
chat_informed: true
source_keys:
  - git-reference
  - github-prs
---

# Branches, commits, and history

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-08-001: Choosing a branch strategy for several related changes, while the project requires a linear history.
- OSS-08-002: Splitting a large experimental commit into reviewable commits, while the branch includes unrelated work from an earlier task.
- OSS-08-003: Rewriting local commit messages before publishing a pull request, while another contributor has based work on the existing commit hashes.
- OSS-08-004: Moving one useful commit onto a clean branch, while the project requires a linear history.
- OSS-08-005: Identifying whether two branches contain the same logical change, while the branch includes unrelated work from an earlier task.
- OSS-08-006: Preserving authorship while combining work from another contributor, while another contributor has based work on the existing commit hashes.
- OSS-08-007: Reviewing how a file evolved across renames, while the project requires a linear history.
- OSS-08-008: Preparing a fix that must target more than one maintained branch, while the branch includes unrelated work from an earlier task.
- OSS-08-009: Removing an accidentally committed generated artifact from history, while another contributor has based work on the existing commit hashes.
- OSS-08-010: Deciding whether a fixup belongs in the current commit or a new commit, while the project requires a linear history.
- OSS-08-011: Choosing a branch strategy for several related changes, while some commits have already been reviewed individually.
- OSS-08-012: Splitting a large experimental commit into reviewable commits, while commit signing is required by repository rules.
- OSS-08-013: Rewriting local commit messages before publishing a pull request, while the change must remain easy to backport later.
- OSS-08-014: Moving one useful commit onto a clean branch, while some commits have already been reviewed individually.
- OSS-08-015: Identifying whether two branches contain the same logical change, while commit signing is required by repository rules.
- OSS-08-016: Preserving authorship while combining work from another contributor, while the change must remain easy to backport later.
- OSS-08-017: Reviewing how a file evolved across renames, while some commits have already been reviewed individually.
- OSS-08-018: Preparing a fix that must target more than one maintained branch, while commit signing is required by repository rules.
- OSS-08-019: Removing an accidentally committed generated artifact from history, while the change must remain easy to backport later.
- OSS-08-020: Deciding whether a fixup belongs in the current commit or a new commit, while some commits have already been reviewed individually.

## Intermediate situations

- OSS-08-021: Choosing a branch strategy for several related changes, while the branch includes unrelated work from an earlier task.
- OSS-08-022: Splitting a large experimental commit into reviewable commits, while another contributor has based work on the existing commit hashes.
- OSS-08-023: Rewriting local commit messages before publishing a pull request, while the project requires a linear history.
- OSS-08-024: Moving one useful commit onto a clean branch, while the branch includes unrelated work from an earlier task.
- OSS-08-025: Identifying whether two branches contain the same logical change, while another contributor has based work on the existing commit hashes.
- OSS-08-026: Preserving authorship while combining work from another contributor, while the project requires a linear history.
- OSS-08-027: Reviewing how a file evolved across renames, while the branch includes unrelated work from an earlier task.
- OSS-08-028: Preparing a fix that must target more than one maintained branch, while another contributor has based work on the existing commit hashes.
- OSS-08-029: Removing an accidentally committed generated artifact from history, while the project requires a linear history.
- OSS-08-030: Deciding whether a fixup belongs in the current commit or a new commit, while the branch includes unrelated work from an earlier task.
- OSS-08-031: Choosing a branch strategy for several related changes, while commit signing is required by repository rules.
- OSS-08-032: Splitting a large experimental commit into reviewable commits, while the change must remain easy to backport later.
- OSS-08-033: Rewriting local commit messages before publishing a pull request, while some commits have already been reviewed individually.
- OSS-08-034: Moving one useful commit onto a clean branch, while commit signing is required by repository rules.
- OSS-08-035: Identifying whether two branches contain the same logical change, while the change must remain easy to backport later.
- OSS-08-036: Preserving authorship while combining work from another contributor, while some commits have already been reviewed individually.
- OSS-08-037: Reviewing how a file evolved across renames, while commit signing is required by repository rules.
- OSS-08-038: Preparing a fix that must target more than one maintained branch, while the change must remain easy to backport later.
- OSS-08-039: Removing an accidentally committed generated artifact from history, while some commits have already been reviewed individually.
- OSS-08-040: Deciding whether a fixup belongs in the current commit or a new commit, while commit signing is required by repository rules.

## Advanced situations

- OSS-08-041: Choosing a branch strategy for several related changes, while another contributor has based work on the existing commit hashes.
- OSS-08-042: Splitting a large experimental commit into reviewable commits, while the project requires a linear history.
- OSS-08-043: Rewriting local commit messages before publishing a pull request, while the branch includes unrelated work from an earlier task.
- OSS-08-044: Moving one useful commit onto a clean branch, while another contributor has based work on the existing commit hashes.
- OSS-08-045: Identifying whether two branches contain the same logical change, while the project requires a linear history.
- OSS-08-046: Preserving authorship while combining work from another contributor, while the branch includes unrelated work from an earlier task.
- OSS-08-047: Reviewing how a file evolved across renames, while another contributor has based work on the existing commit hashes.
- OSS-08-048: Preparing a fix that must target more than one maintained branch, while the project requires a linear history.
- OSS-08-049: Removing an accidentally committed generated artifact from history, while the branch includes unrelated work from an earlier task.
- OSS-08-050: Deciding whether a fixup belongs in the current commit or a new commit, while another contributor has based work on the existing commit hashes.
