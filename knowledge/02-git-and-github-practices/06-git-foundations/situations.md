---
title: Git foundations
group: 02-git-and-github-practices
category: 06-git-foundations
journey_stage: contribute
scenario_count: 50
content_type: situations-only
chat_informed: true
source_keys:
  - git-reference
  - github-project
---

# Git foundations

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-06-001: Checking which files have changed before creating a commit, while the working tree contains edits from several experiments.
- OSS-06-002: Separating intended edits from generated or temporary files, while the repository uses hooks that modify staged content.
- OSS-06-003: Restoring one file without losing unrelated local work, while a command produced an unexpected detached HEAD state.
- OSS-06-004: Understanding the difference between the working tree and staging area, while the working tree contains edits from several experiments.
- OSS-06-005: Creating a commit that contains one logical change, while the repository uses hooks that modify staged content.
- OSS-06-006: Viewing the exact patch that will be submitted, while a command produced an unexpected detached HEAD state.
- OSS-06-007: Finding the commit that introduced a behavior, while the working tree contains edits from several experiments.
- OSS-06-008: Moving unfinished work aside to inspect another branch, while the repository uses hooks that modify staged content.
- OSS-06-009: Correcting author identity before publishing commits, while a command produced an unexpected detached HEAD state.
- OSS-06-010: Recovering a change after an accidental local operation, while the working tree contains edits from several experiments.
- OSS-06-011: Checking which files have changed before creating a commit, while generated files look similar to manually maintained files.
- OSS-06-012: Separating intended edits from generated or temporary files, while their global Git configuration conflicts with project expectations.
- OSS-06-013: Restoring one file without losing unrelated local work, while they are unsure which recovery command preserves uncommitted work.
- OSS-06-014: Understanding the difference between the working tree and staging area, while generated files look similar to manually maintained files.
- OSS-06-015: Creating a commit that contains one logical change, while their global Git configuration conflicts with project expectations.
- OSS-06-016: Viewing the exact patch that will be submitted, while they are unsure which recovery command preserves uncommitted work.
- OSS-06-017: Finding the commit that introduced a behavior, while generated files look similar to manually maintained files.
- OSS-06-018: Moving unfinished work aside to inspect another branch, while their global Git configuration conflicts with project expectations.
- OSS-06-019: Correcting author identity before publishing commits, while they are unsure which recovery command preserves uncommitted work.
- OSS-06-020: Recovering a change after an accidental local operation, while generated files look similar to manually maintained files.

## Intermediate situations

- OSS-06-021: Checking which files have changed before creating a commit, while the repository uses hooks that modify staged content.
- OSS-06-022: Separating intended edits from generated or temporary files, while a command produced an unexpected detached HEAD state.
- OSS-06-023: Restoring one file without losing unrelated local work, while the working tree contains edits from several experiments.
- OSS-06-024: Understanding the difference between the working tree and staging area, while the repository uses hooks that modify staged content.
- OSS-06-025: Creating a commit that contains one logical change, while a command produced an unexpected detached HEAD state.
- OSS-06-026: Viewing the exact patch that will be submitted, while the working tree contains edits from several experiments.
- OSS-06-027: Finding the commit that introduced a behavior, while the repository uses hooks that modify staged content.
- OSS-06-028: Moving unfinished work aside to inspect another branch, while a command produced an unexpected detached HEAD state.
- OSS-06-029: Correcting author identity before publishing commits, while the working tree contains edits from several experiments.
- OSS-06-030: Recovering a change after an accidental local operation, while the repository uses hooks that modify staged content.
- OSS-06-031: Checking which files have changed before creating a commit, while their global Git configuration conflicts with project expectations.
- OSS-06-032: Separating intended edits from generated or temporary files, while they are unsure which recovery command preserves uncommitted work.
- OSS-06-033: Restoring one file without losing unrelated local work, while generated files look similar to manually maintained files.
- OSS-06-034: Understanding the difference between the working tree and staging area, while their global Git configuration conflicts with project expectations.
- OSS-06-035: Creating a commit that contains one logical change, while they are unsure which recovery command preserves uncommitted work.
- OSS-06-036: Viewing the exact patch that will be submitted, while generated files look similar to manually maintained files.
- OSS-06-037: Finding the commit that introduced a behavior, while their global Git configuration conflicts with project expectations.
- OSS-06-038: Moving unfinished work aside to inspect another branch, while they are unsure which recovery command preserves uncommitted work.
- OSS-06-039: Correcting author identity before publishing commits, while generated files look similar to manually maintained files.
- OSS-06-040: Recovering a change after an accidental local operation, while their global Git configuration conflicts with project expectations.

## Advanced situations

- OSS-06-041: Checking which files have changed before creating a commit, while a command produced an unexpected detached HEAD state.
- OSS-06-042: Separating intended edits from generated or temporary files, while the working tree contains edits from several experiments.
- OSS-06-043: Restoring one file without losing unrelated local work, while the repository uses hooks that modify staged content.
- OSS-06-044: Understanding the difference between the working tree and staging area, while a command produced an unexpected detached HEAD state.
- OSS-06-045: Creating a commit that contains one logical change, while the working tree contains edits from several experiments.
- OSS-06-046: Viewing the exact patch that will be submitted, while the repository uses hooks that modify staged content.
- OSS-06-047: Finding the commit that introduced a behavior, while a command produced an unexpected detached HEAD state.
- OSS-06-048: Moving unfinished work aside to inspect another branch, while the working tree contains edits from several experiments.
- OSS-06-049: Correcting author identity before publishing commits, while the repository uses hooks that modify staged content.
- OSS-06-050: Recovering a change after an accidental local operation, while a command produced an unexpected detached HEAD state.
