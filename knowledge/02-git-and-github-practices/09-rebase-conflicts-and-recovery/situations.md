---
title: Rebase, conflicts, and recovery
group: 02-git-and-github-practices
category: 09-rebase-conflicts-and-recovery
journey_stage: contribute
scenario_count: 50
content_type: situations-only
chat_informed: true
source_keys:
  - git-reference
  - github-prs
---

# Rebase, conflicts, and recovery

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-09-001: Rebasing a feature branch onto a rapidly changing default branch, while tests pass on both original branches but fail after resolution.
- OSS-09-002: Resolving a conflict where one side deleted a file, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-003: Continuing work after a rebase stops midway, while the repository regenerates conflicted files from another source.
- OSS-09-004: Recovering commits after resetting the wrong branch, while tests pass on both original branches but fail after resolution.
- OSS-09-005: Deciding which side of a conflict reflects current project intent, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-006: Handling conflicts inside generated lock or manifest files, while the repository regenerates conflicted files from another source.
- OSS-09-007: Rebasing a stack of dependent pull requests, while tests pass on both original branches but fail after resolution.
- OSS-09-008: Recovering from an interrupted cherry-pick, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-009: Comparing the rewritten branch with its pre-rebase state, while the repository regenerates conflicted files from another source.
- OSS-09-010: Updating a pull request after upstream refactors the same component, while tests pass on both original branches but fail after resolution.
- OSS-09-011: Rebasing a feature branch onto a rapidly changing default branch, while the conflict involves code they did not originally write.
- OSS-09-012: Resolving a conflict where one side deleted a file, while reflog contains several similarly named recovery points.
- OSS-09-013: Continuing work after a rebase stops midway, while review comments refer to commits that no longer exist.
- OSS-09-014: Recovering commits after resetting the wrong branch, while the conflict involves code they did not originally write.
- OSS-09-015: Deciding which side of a conflict reflects current project intent, while reflog contains several similarly named recovery points.
- OSS-09-016: Handling conflicts inside generated lock or manifest files, while review comments refer to commits that no longer exist.
- OSS-09-017: Rebasing a stack of dependent pull requests, while the conflict involves code they did not originally write.
- OSS-09-018: Recovering from an interrupted cherry-pick, while reflog contains several similarly named recovery points.
- OSS-09-019: Comparing the rewritten branch with its pre-rebase state, while review comments refer to commits that no longer exist.
- OSS-09-020: Updating a pull request after upstream refactors the same component, while the conflict involves code they did not originally write.

## Intermediate situations

- OSS-09-021: Rebasing a feature branch onto a rapidly changing default branch, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-022: Resolving a conflict where one side deleted a file, while the repository regenerates conflicted files from another source.
- OSS-09-023: Continuing work after a rebase stops midway, while tests pass on both original branches but fail after resolution.
- OSS-09-024: Recovering commits after resetting the wrong branch, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-025: Deciding which side of a conflict reflects current project intent, while the repository regenerates conflicted files from another source.
- OSS-09-026: Handling conflicts inside generated lock or manifest files, while tests pass on both original branches but fail after resolution.
- OSS-09-027: Rebasing a stack of dependent pull requests, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-028: Recovering from an interrupted cherry-pick, while the repository regenerates conflicted files from another source.
- OSS-09-029: Comparing the rewritten branch with its pre-rebase state, while tests pass on both original branches but fail after resolution.
- OSS-09-030: Updating a pull request after upstream refactors the same component, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-031: Rebasing a feature branch onto a rapidly changing default branch, while reflog contains several similarly named recovery points.
- OSS-09-032: Resolving a conflict where one side deleted a file, while review comments refer to commits that no longer exist.
- OSS-09-033: Continuing work after a rebase stops midway, while the conflict involves code they did not originally write.
- OSS-09-034: Recovering commits after resetting the wrong branch, while reflog contains several similarly named recovery points.
- OSS-09-035: Deciding which side of a conflict reflects current project intent, while review comments refer to commits that no longer exist.
- OSS-09-036: Handling conflicts inside generated lock or manifest files, while the conflict involves code they did not originally write.
- OSS-09-037: Rebasing a stack of dependent pull requests, while reflog contains several similarly named recovery points.
- OSS-09-038: Recovering from an interrupted cherry-pick, while review comments refer to commits that no longer exist.
- OSS-09-039: Comparing the rewritten branch with its pre-rebase state, while the conflict involves code they did not originally write.
- OSS-09-040: Updating a pull request after upstream refactors the same component, while reflog contains several similarly named recovery points.

## Advanced situations

- OSS-09-041: Rebasing a feature branch onto a rapidly changing default branch, while the repository regenerates conflicted files from another source.
- OSS-09-042: Resolving a conflict where one side deleted a file, while tests pass on both original branches but fail after resolution.
- OSS-09-043: Continuing work after a rebase stops midway, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-044: Recovering commits after resetting the wrong branch, while the repository regenerates conflicted files from another source.
- OSS-09-045: Deciding which side of a conflict reflects current project intent, while tests pass on both original branches but fail after resolution.
- OSS-09-046: Handling conflicts inside generated lock or manifest files, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-047: Rebasing a stack of dependent pull requests, while the repository regenerates conflicted files from another source.
- OSS-09-048: Recovering from an interrupted cherry-pick, while tests pass on both original branches but fail after resolution.
- OSS-09-049: Comparing the rewritten branch with its pre-rebase state, while the remote branch has already been force-pushed by a collaborator.
- OSS-09-050: Updating a pull request after upstream refactors the same component, while the repository regenerates conflicted files from another source.
