---
title: Cloud-native and infrastructure contributions
group: 10-advanced-engineering-and-project-lifecycle
category: 49-cloud-native-infrastructure-contributions
journey_stage: ecosystem
scenario_count: 50
content_type: situations-only
chat_informed: true
source_keys:
  - cncf-getting-started
  - kubernetes-contribute
  - kubeflow-contributing
---

# Cloud-native and infrastructure contributions

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-49-001: Reproducing a controller bug in a minimal cluster, while the full integration environment is expensive to reproduce.
- OSS-49-002: Changing a custom resource schema while preserving upgrades, while the change affects both namespaced and cluster-wide resources.
- OSS-49-003: Testing behavior across multiple Kubernetes versions, while logs from the affected environment contain sensitive details.
- OSS-49-004: Debugging reconciliation that repeatedly retries, while the full integration environment is expensive to reproduce.
- OSS-49-005: Contributing deployment manifests for restricted environments, while the change affects both namespaced and cluster-wide resources.
- OSS-49-006: Reviewing RBAC permissions added by a new feature, while logs from the affected environment contain sensitive details.
- OSS-49-007: Handling a change that requires coordinated updates across operators, while the full integration environment is expensive to reproduce.
- OSS-49-008: Testing failure recovery when an external service is unavailable, while the change affects both namespaced and cluster-wide resources.
- OSS-49-009: Documenting resource requirements for small clusters, while logs from the affected environment contain sensitive details.
- OSS-49-010: Tracing an issue across infrastructure, API, and user-interface layers, while the full integration environment is expensive to reproduce.
- OSS-49-011: Reproducing a controller bug in a minimal cluster, while cluster state differs from the declarative configuration.
- OSS-49-012: Changing a custom resource schema while preserving upgrades, while supported platform versions have different APIs.
- OSS-49-013: Testing behavior across multiple Kubernetes versions, while ownership is divided across several repositories and SIGs.
- OSS-49-014: Debugging reconciliation that repeatedly retries, while cluster state differs from the declarative configuration.
- OSS-49-015: Contributing deployment manifests for restricted environments, while supported platform versions have different APIs.
- OSS-49-016: Reviewing RBAC permissions added by a new feature, while ownership is divided across several repositories and SIGs.
- OSS-49-017: Handling a change that requires coordinated updates across operators, while cluster state differs from the declarative configuration.
- OSS-49-018: Testing failure recovery when an external service is unavailable, while supported platform versions have different APIs.
- OSS-49-019: Documenting resource requirements for small clusters, while ownership is divided across several repositories and SIGs.
- OSS-49-020: Tracing an issue across infrastructure, API, and user-interface layers, while cluster state differs from the declarative configuration.

## Intermediate situations

- OSS-49-021: Reproducing a controller bug in a minimal cluster, while the change affects both namespaced and cluster-wide resources.
- OSS-49-022: Changing a custom resource schema while preserving upgrades, while logs from the affected environment contain sensitive details.
- OSS-49-023: Testing behavior across multiple Kubernetes versions, while the full integration environment is expensive to reproduce.
- OSS-49-024: Debugging reconciliation that repeatedly retries, while the change affects both namespaced and cluster-wide resources.
- OSS-49-025: Contributing deployment manifests for restricted environments, while logs from the affected environment contain sensitive details.
- OSS-49-026: Reviewing RBAC permissions added by a new feature, while the full integration environment is expensive to reproduce.
- OSS-49-027: Handling a change that requires coordinated updates across operators, while the change affects both namespaced and cluster-wide resources.
- OSS-49-028: Testing failure recovery when an external service is unavailable, while logs from the affected environment contain sensitive details.
- OSS-49-029: Documenting resource requirements for small clusters, while the full integration environment is expensive to reproduce.
- OSS-49-030: Tracing an issue across infrastructure, API, and user-interface layers, while the change affects both namespaced and cluster-wide resources.
- OSS-49-031: Reproducing a controller bug in a minimal cluster, while supported platform versions have different APIs.
- OSS-49-032: Changing a custom resource schema while preserving upgrades, while ownership is divided across several repositories and SIGs.
- OSS-49-033: Testing behavior across multiple Kubernetes versions, while cluster state differs from the declarative configuration.
- OSS-49-034: Debugging reconciliation that repeatedly retries, while supported platform versions have different APIs.
- OSS-49-035: Contributing deployment manifests for restricted environments, while ownership is divided across several repositories and SIGs.
- OSS-49-036: Reviewing RBAC permissions added by a new feature, while cluster state differs from the declarative configuration.
- OSS-49-037: Handling a change that requires coordinated updates across operators, while supported platform versions have different APIs.
- OSS-49-038: Testing failure recovery when an external service is unavailable, while ownership is divided across several repositories and SIGs.
- OSS-49-039: Documenting resource requirements for small clusters, while cluster state differs from the declarative configuration.
- OSS-49-040: Tracing an issue across infrastructure, API, and user-interface layers, while supported platform versions have different APIs.

## Advanced situations

- OSS-49-041: Reproducing a controller bug in a minimal cluster, while logs from the affected environment contain sensitive details.
- OSS-49-042: Changing a custom resource schema while preserving upgrades, while the full integration environment is expensive to reproduce.
- OSS-49-043: Testing behavior across multiple Kubernetes versions, while the change affects both namespaced and cluster-wide resources.
- OSS-49-044: Debugging reconciliation that repeatedly retries, while logs from the affected environment contain sensitive details.
- OSS-49-045: Contributing deployment manifests for restricted environments, while the full integration environment is expensive to reproduce.
- OSS-49-046: Reviewing RBAC permissions added by a new feature, while the change affects both namespaced and cluster-wide resources.
- OSS-49-047: Handling a change that requires coordinated updates across operators, while logs from the affected environment contain sensitive details.
- OSS-49-048: Testing failure recovery when an external service is unavailable, while the full integration environment is expensive to reproduce.
- OSS-49-049: Documenting resource requirements for small clusters, while the change affects both namespaced and cluster-wide resources.
- OSS-49-050: Tracing an issue across infrastructure, API, and user-interface layers, while logs from the affected environment contain sensitive details.
