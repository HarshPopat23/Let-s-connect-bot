---
title: Data, AI, and machine-learning projects
group: 10-advanced-engineering-and-project-lifecycle
category: 48-data-ai-and-machine-learning-projects
journey_stage: contribute
scenario_count: 50
content_type: situations-only
chat_informed: false
source_keys:
  - github-project
  - legal-guide
  - openssf-baseline
---

# Data, AI, and machine-learning projects

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-48-001: Contributing a dataset with unclear collection consent, while the training data provenance is incomplete.
- OSS-48-002: Reproducing a model result without the original hardware, while the evaluation metric hides important failure modes.
- OSS-48-003: Reviewing an evaluation that uses a contaminated benchmark, while model behavior changes across runtime versions.
- OSS-48-004: Adding a model whose weights have different licensing terms, while the training data provenance is incomplete.
- OSS-48-005: Reporting bias that appears for a specific user group, while the evaluation metric hides important failure modes.
- OSS-48-006: Updating preprocessing code that changes historical results, while model behavior changes across runtime versions.
- OSS-48-007: Contributing an inference optimization with accuracy tradeoffs, while the training data provenance is incomplete.
- OSS-48-008: Documenting nondeterministic training behavior, while the evaluation metric hides important failure modes.
- OSS-48-009: Reviewing generated labels created by a proprietary service, while model behavior changes across runtime versions.
- OSS-48-010: Adding a large artifact that does not fit normal Git workflows, while the training data provenance is incomplete.
- OSS-48-011: Contributing a dataset with unclear collection consent, while results depend on unavailable random seeds or checkpoints.
- OSS-48-012: Reproducing a model result without the original hardware, while storage and bandwidth costs affect reproducibility.
- OSS-48-013: Reviewing an evaluation that uses a contaminated benchmark, while privacy constraints limit the evidence that can be published.
- OSS-48-014: Adding a model whose weights have different licensing terms, while results depend on unavailable random seeds or checkpoints.
- OSS-48-015: Reporting bias that appears for a specific user group, while storage and bandwidth costs affect reproducibility.
- OSS-48-016: Updating preprocessing code that changes historical results, while privacy constraints limit the evidence that can be published.
- OSS-48-017: Contributing an inference optimization with accuracy tradeoffs, while results depend on unavailable random seeds or checkpoints.
- OSS-48-018: Documenting nondeterministic training behavior, while storage and bandwidth costs affect reproducibility.
- OSS-48-019: Reviewing generated labels created by a proprietary service, while privacy constraints limit the evidence that can be published.
- OSS-48-020: Adding a large artifact that does not fit normal Git workflows, while results depend on unavailable random seeds or checkpoints.

## Intermediate situations

- OSS-48-021: Contributing a dataset with unclear collection consent, while the evaluation metric hides important failure modes.
- OSS-48-022: Reproducing a model result without the original hardware, while model behavior changes across runtime versions.
- OSS-48-023: Reviewing an evaluation that uses a contaminated benchmark, while the training data provenance is incomplete.
- OSS-48-024: Adding a model whose weights have different licensing terms, while the evaluation metric hides important failure modes.
- OSS-48-025: Reporting bias that appears for a specific user group, while model behavior changes across runtime versions.
- OSS-48-026: Updating preprocessing code that changes historical results, while the training data provenance is incomplete.
- OSS-48-027: Contributing an inference optimization with accuracy tradeoffs, while the evaluation metric hides important failure modes.
- OSS-48-028: Documenting nondeterministic training behavior, while model behavior changes across runtime versions.
- OSS-48-029: Reviewing generated labels created by a proprietary service, while the training data provenance is incomplete.
- OSS-48-030: Adding a large artifact that does not fit normal Git workflows, while the evaluation metric hides important failure modes.
- OSS-48-031: Contributing a dataset with unclear collection consent, while storage and bandwidth costs affect reproducibility.
- OSS-48-032: Reproducing a model result without the original hardware, while privacy constraints limit the evidence that can be published.
- OSS-48-033: Reviewing an evaluation that uses a contaminated benchmark, while results depend on unavailable random seeds or checkpoints.
- OSS-48-034: Adding a model whose weights have different licensing terms, while storage and bandwidth costs affect reproducibility.
- OSS-48-035: Reporting bias that appears for a specific user group, while privacy constraints limit the evidence that can be published.
- OSS-48-036: Updating preprocessing code that changes historical results, while results depend on unavailable random seeds or checkpoints.
- OSS-48-037: Contributing an inference optimization with accuracy tradeoffs, while storage and bandwidth costs affect reproducibility.
- OSS-48-038: Documenting nondeterministic training behavior, while privacy constraints limit the evidence that can be published.
- OSS-48-039: Reviewing generated labels created by a proprietary service, while results depend on unavailable random seeds or checkpoints.
- OSS-48-040: Adding a large artifact that does not fit normal Git workflows, while storage and bandwidth costs affect reproducibility.

## Advanced situations

- OSS-48-041: Contributing a dataset with unclear collection consent, while model behavior changes across runtime versions.
- OSS-48-042: Reproducing a model result without the original hardware, while the training data provenance is incomplete.
- OSS-48-043: Reviewing an evaluation that uses a contaminated benchmark, while the evaluation metric hides important failure modes.
- OSS-48-044: Adding a model whose weights have different licensing terms, while model behavior changes across runtime versions.
- OSS-48-045: Reporting bias that appears for a specific user group, while the training data provenance is incomplete.
- OSS-48-046: Updating preprocessing code that changes historical results, while the evaluation metric hides important failure modes.
- OSS-48-047: Contributing an inference optimization with accuracy tradeoffs, while model behavior changes across runtime versions.
- OSS-48-048: Documenting nondeterministic training behavior, while the training data provenance is incomplete.
- OSS-48-049: Reviewing generated labels created by a proprietary service, while the evaluation metric hides important failure modes.
- OSS-48-050: Adding a large artifact that does not fit normal Git workflows, while model behavior changes across runtime versions.
