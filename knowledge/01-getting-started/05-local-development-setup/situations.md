---
title: Local development setup
group: 01-getting-started
category: 05-local-development-setup
journey_stage: learn
scenario_count: 50
content_type: situations-only
chat_informed: true
source_keys:
  - github-project
  - open-source-guide
---

# Local development setup

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-05-001: Setting up the project from a clean machine, while the setup guide omits a system dependency.
- OSS-05-002: Trying to reproduce the documented development environment, while the full build exceeds available memory or disk.
- OSS-05-003: Building only the component related to an issue, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-004: Running the repository in a containerized environment, while the setup guide omits a system dependency.
- OSS-05-005: Configuring required services without access to production credentials, while the full build exceeds available memory or disk.
- OSS-05-006: Using a different operating system from most maintainers, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-007: Installing language toolchains required by different subprojects, while the setup guide omits a system dependency.
- OSS-05-008: Running local checks before making any code change, while the full build exceeds available memory or disk.
- OSS-05-009: Resetting a partially broken development environment, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-010: Documenting setup steps discovered through experimentation, while the setup guide omits a system dependency.
- OSS-05-011: Setting up the project from a clean machine, while the documented tool version is no longer available.
- OSS-05-012: Trying to reproduce the documented development environment, while environment variables are described but sample values are missing.
- OSS-05-013: Building only the component related to an issue, while network restrictions prevent downloading a required dependency.
- OSS-05-014: Running the repository in a containerized environment, while the documented tool version is no longer available.
- OSS-05-015: Configuring required services without access to production credentials, while environment variables are described but sample values are missing.
- OSS-05-016: Using a different operating system from most maintainers, while network restrictions prevent downloading a required dependency.
- OSS-05-017: Installing language toolchains required by different subprojects, while the documented tool version is no longer available.
- OSS-05-018: Running local checks before making any code change, while environment variables are described but sample values are missing.
- OSS-05-019: Resetting a partially broken development environment, while network restrictions prevent downloading a required dependency.
- OSS-05-020: Documenting setup steps discovered through experimentation, while the documented tool version is no longer available.

## Intermediate situations

- OSS-05-021: Setting up the project from a clean machine, while the full build exceeds available memory or disk.
- OSS-05-022: Trying to reproduce the documented development environment, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-023: Building only the component related to an issue, while the setup guide omits a system dependency.
- OSS-05-024: Running the repository in a containerized environment, while the full build exceeds available memory or disk.
- OSS-05-025: Configuring required services without access to production credentials, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-026: Using a different operating system from most maintainers, while the setup guide omits a system dependency.
- OSS-05-027: Installing language toolchains required by different subprojects, while the full build exceeds available memory or disk.
- OSS-05-028: Running local checks before making any code change, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-029: Resetting a partially broken development environment, while the setup guide omits a system dependency.
- OSS-05-030: Documenting setup steps discovered through experimentation, while the full build exceeds available memory or disk.
- OSS-05-031: Setting up the project from a clean machine, while environment variables are described but sample values are missing.
- OSS-05-032: Trying to reproduce the documented development environment, while network restrictions prevent downloading a required dependency.
- OSS-05-033: Building only the component related to an issue, while the documented tool version is no longer available.
- OSS-05-034: Running the repository in a containerized environment, while environment variables are described but sample values are missing.
- OSS-05-035: Configuring required services without access to production credentials, while network restrictions prevent downloading a required dependency.
- OSS-05-036: Using a different operating system from most maintainers, while the documented tool version is no longer available.
- OSS-05-037: Installing language toolchains required by different subprojects, while environment variables are described but sample values are missing.
- OSS-05-038: Running local checks before making any code change, while network restrictions prevent downloading a required dependency.
- OSS-05-039: Resetting a partially broken development environment, while the documented tool version is no longer available.
- OSS-05-040: Documenting setup steps discovered through experimentation, while environment variables are described but sample values are missing.

## Advanced situations

- OSS-05-041: Setting up the project from a clean machine, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-042: Trying to reproduce the documented development environment, while the setup guide omits a system dependency.
- OSS-05-043: Building only the component related to an issue, while the full build exceeds available memory or disk.
- OSS-05-044: Running the repository in a containerized environment, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-045: Configuring required services without access to production credentials, while the setup guide omits a system dependency.
- OSS-05-046: Using a different operating system from most maintainers, while the full build exceeds available memory or disk.
- OSS-05-047: Installing language toolchains required by different subprojects, while a setup script succeeds for maintainers but fails on a fresh machine.
- OSS-05-048: Running local checks before making any code change, while the setup guide omits a system dependency.
- OSS-05-049: Resetting a partially broken development environment, while the full build exceeds available memory or disk.
- OSS-05-050: Documenting setup steps discovered through experimentation, while a setup script succeeds for maintainers but fails on a fresh machine.
