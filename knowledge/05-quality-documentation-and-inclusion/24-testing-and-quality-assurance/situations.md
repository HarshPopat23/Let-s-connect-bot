---
title: Testing and quality assurance
group: 05-quality-documentation-and-inclusion
category: 24-testing-and-quality-assurance
journey_stage: contribute
scenario_count: 50
content_type: situations-only
chat_informed: true
source_keys:
  - github-project
  - github-actions
  - openssf-baseline
---

# Testing and quality assurance

This file contains situations only. It intentionally provides no answers or recommended actions.

## Beginner situations

- OSS-24-001: Designing a regression test before changing implementation code, while the full test environment is expensive to run locally.
- OSS-24-002: Testing behavior across supported operating systems, while the failure depends on external network availability.
- OSS-24-003: Adding coverage for an error path that is difficult to trigger, while test fixtures contain outdated assumptions.
- OSS-24-004: Deciding the appropriate level for a new test, while the full test environment is expensive to run locally.
- OSS-24-005: Reproducing a race condition with unreliable timing, while the failure depends on external network availability.
- OSS-24-006: Testing compatibility with multiple dependency versions, while test fixtures contain outdated assumptions.
- OSS-24-007: Validating a user-interface change with assistive technology, while the full test environment is expensive to run locally.
- OSS-24-008: Testing an upgrade path from an older release, while the failure depends on external network availability.
- OSS-24-009: Reviewing whether a mock hides important integration behavior, while test fixtures contain outdated assumptions.
- OSS-24-010: Reducing a slow test without weakening its assertion, while the full test environment is expensive to run locally.
- OSS-24-011: Designing a regression test before changing implementation code, while existing tests encode undocumented behavior.
- OSS-24-012: Testing behavior across supported operating systems, while coverage improves while meaningful assertions remain weak.
- OSS-24-013: Adding coverage for an error path that is difficult to trigger, while the project supports platforms unavailable to the contributor.
- OSS-24-014: Deciding the appropriate level for a new test, while existing tests encode undocumented behavior.
- OSS-24-015: Reproducing a race condition with unreliable timing, while coverage improves while meaningful assertions remain weak.
- OSS-24-016: Testing compatibility with multiple dependency versions, while the project supports platforms unavailable to the contributor.
- OSS-24-017: Validating a user-interface change with assistive technology, while existing tests encode undocumented behavior.
- OSS-24-018: Testing an upgrade path from an older release, while coverage improves while meaningful assertions remain weak.
- OSS-24-019: Reviewing whether a mock hides important integration behavior, while the project supports platforms unavailable to the contributor.
- OSS-24-020: Reducing a slow test without weakening its assertion, while existing tests encode undocumented behavior.

## Intermediate situations

- OSS-24-021: Designing a regression test before changing implementation code, while the failure depends on external network availability.
- OSS-24-022: Testing behavior across supported operating systems, while test fixtures contain outdated assumptions.
- OSS-24-023: Adding coverage for an error path that is difficult to trigger, while the full test environment is expensive to run locally.
- OSS-24-024: Deciding the appropriate level for a new test, while the failure depends on external network availability.
- OSS-24-025: Reproducing a race condition with unreliable timing, while test fixtures contain outdated assumptions.
- OSS-24-026: Testing compatibility with multiple dependency versions, while the full test environment is expensive to run locally.
- OSS-24-027: Validating a user-interface change with assistive technology, while the failure depends on external network availability.
- OSS-24-028: Testing an upgrade path from an older release, while test fixtures contain outdated assumptions.
- OSS-24-029: Reviewing whether a mock hides important integration behavior, while the full test environment is expensive to run locally.
- OSS-24-030: Reducing a slow test without weakening its assertion, while the failure depends on external network availability.
- OSS-24-031: Designing a regression test before changing implementation code, while coverage improves while meaningful assertions remain weak.
- OSS-24-032: Testing behavior across supported operating systems, while the project supports platforms unavailable to the contributor.
- OSS-24-033: Adding coverage for an error path that is difficult to trigger, while existing tests encode undocumented behavior.
- OSS-24-034: Deciding the appropriate level for a new test, while coverage improves while meaningful assertions remain weak.
- OSS-24-035: Reproducing a race condition with unreliable timing, while the project supports platforms unavailable to the contributor.
- OSS-24-036: Testing compatibility with multiple dependency versions, while existing tests encode undocumented behavior.
- OSS-24-037: Validating a user-interface change with assistive technology, while coverage improves while meaningful assertions remain weak.
- OSS-24-038: Testing an upgrade path from an older release, while the project supports platforms unavailable to the contributor.
- OSS-24-039: Reviewing whether a mock hides important integration behavior, while existing tests encode undocumented behavior.
- OSS-24-040: Reducing a slow test without weakening its assertion, while coverage improves while meaningful assertions remain weak.

## Advanced situations

- OSS-24-041: Designing a regression test before changing implementation code, while test fixtures contain outdated assumptions.
- OSS-24-042: Testing behavior across supported operating systems, while the full test environment is expensive to run locally.
- OSS-24-043: Adding coverage for an error path that is difficult to trigger, while the failure depends on external network availability.
- OSS-24-044: Deciding the appropriate level for a new test, while test fixtures contain outdated assumptions.
- OSS-24-045: Reproducing a race condition with unreliable timing, while the full test environment is expensive to run locally.
- OSS-24-046: Testing compatibility with multiple dependency versions, while the failure depends on external network availability.
- OSS-24-047: Validating a user-interface change with assistive technology, while test fixtures contain outdated assumptions.
- OSS-24-048: Testing an upgrade path from an older release, while the full test environment is expensive to run locally.
- OSS-24-049: Reviewing whether a mock hides important integration behavior, while the failure depends on external network availability.
- OSS-24-050: Reducing a slow test without weakening its assertion, while test fixtures contain outdated assumptions.
